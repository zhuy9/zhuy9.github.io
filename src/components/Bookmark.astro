---
interface Props {
  href: string;
  title: string;
  source: string;
}
const { href, title, source } = Astro.props;
---

<a class="bookmark" href={href} target="_blank" rel="noopener noreferrer">
  <span class="bookmark-title">{title}</span>
  <span class="bookmark-source">{source}</span>
</a>
